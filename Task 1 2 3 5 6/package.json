{
  "name": "implement-authentication-and-authorization",
  "version": "1.0.0",
  "description": "1. Use JSON Web Tokens (JWT) to secure the API endpoints.\r 2. Create an endpoint for users to sign up and login, and return a JWT upon successful authentication.\r 3. Add middleware to verify the JWT on all protected endpoints.\r 4. Implement proper error handling for authentication failures.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1310.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.7.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.9.0",
    "multer": "^1.4.5-lts.1",
    "winston": "^3.8.2"
  }
}
